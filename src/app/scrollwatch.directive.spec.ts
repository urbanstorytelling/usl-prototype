import { ScrollwatchDirective } from './scrollwatch.directive';

describe('ScrollwatchDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollwatchDirective();
    expect(directive).toBeTruthy();
  });
});
