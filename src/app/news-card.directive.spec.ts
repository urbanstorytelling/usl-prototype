import { NewsCardDirective } from './news-card.directive';

describe('NewsCardDirective', () => {
  it('should create an instance', () => {
    const directive = new NewsCardDirective();
    expect(directive).toBeTruthy();
  });
});
